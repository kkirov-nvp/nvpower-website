# Security headers — Netlify / Cloudflare Pages format.
#
# ⚠️  This site is deployed on VERCEL, which ignores this file entirely — the
# live headers come from /vercel.json → "headers". This file is kept only so a
# move to Netlify / Cloudflare Pages still works. Change one, change the other.
# For nginx, use add_header.
# When you wire a real form endpoint, add its origin to connect-src + form-action.

/*
  X-Content-Type-Options: nosniff
  X-Frame-Options: DENY
  Referrer-Policy: strict-origin-when-cross-origin
  Permissions-Policy: camera=(), microphone=(), geolocation=()
  Strict-Transport-Security: max-age=31536000; includeSubDomains; preload
  Content-Security-Policy: default-src 'self'; script-src 'self' 'unsafe-inline' https://www.googletagmanager.com; style-src 'self' 'unsafe-inline'; img-src 'self' data: https://www.google-analytics.com; font-src 'self'; frame-src https://www.google.com; connect-src 'self' https://www.google-analytics.com; base-uri 'self'; form-action 'self'; object-src 'none'

/_astro/*
  Cache-Control: public, max-age=31536000, immutable
